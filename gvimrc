if has("gui_running")
   " set guioptions-=T " no toolbar.
"   set guioptions-=h " no horizontal scroll.
   set guioptions-=v " no vertical scroll.
  " set guioptions-=r " no right scrollbar.
   set guioptions-=L " no left scrollbar.
   set guifont=monospace\ 11
endif
